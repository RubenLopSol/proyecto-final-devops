apiVersion: v1
kind: Service
metadata:
  name: openpanel-api
  namespace: openpanel
  labels:
    app: openpanel-api
spec:
  type: ClusterIP
  ports:
    - port: 3333
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: openpanel-api
    version: blue
