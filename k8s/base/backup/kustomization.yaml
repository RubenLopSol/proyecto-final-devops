apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: backup

resources:
  # MinIO
  - minio/pvc.yaml
  - minio/deployment.yaml
  - minio/service.yaml
  # Velero
  - velero/backup-location.yaml
  - velero/schedule.yaml

commonLabels:
  project: openpanel-backup
