# =============================================================================
# Loki — Helm values
# Chart: grafana/loki
# =============================================================================

# Modo simple para entorno local (single binary)
deploymentMode: SingleBinary

loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1
  storage:
    type: filesystem
  # Use in-memory ring for ALL components. With replication_factor=1 and a
  # single StatefulSet pod, memberlist gossip is unnecessary and causes a
  # ring deadlock on pod restarts (stale unhealthy entry blocks readiness
  # indefinitely because the restarted pod gets the same IP).
  # Every component that exposes a ring must be listed here; missing one
  # leaves it on the memberlist default and re-introduces the deadlock.
  ingester:
    lifecycler:
      ring:
        kvstore:
          store: inmemory
  distributor:
    ring:
      kvstore:
        store: inmemory
  ruler:
    ring:
      kvstore:
        store: inmemory
  # schema_config is required by Loki chart >= 6.0.
  # Without it the Helm template fails to render and ArgoCD shows Unknown sync status.
  # v13 + tsdb is the current recommended schema for new installations.
  schemaConfig:
    configs:
      - from: "2024-04-01"
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: index_
          period: 24h

singleBinary:
  replicas: 1
  nodeSelector:
    workload: observability
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

# Disable memberlist gossip — all rings use inmemory, so there are no peers to join.
# Without this, Loki still initialises memberlist and may attempt to connect to
# the loki-memberlist headless service, causing noise in logs.
memberlist:
  join_members: []

# Desactivar componentes de modo distribuido (no necesarios en local)
read:
  replicas: 0
write:
  replicas: 0
backend:
  replicas: 0
