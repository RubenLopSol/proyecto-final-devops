# =============================================================================
# STAGING overlay — observability stack aggregator
#
# Renders all four charts (kube-prometheus-stack, loki, promtail, tempo) via
# helmChartInflationGenerator. Each chart has its own subdirectory with
# staging-specific values layered on top of the base values.
#
# Build all:   kustomize build --enable-helm k8s/infrastructure/overlays/staging/observability
# Build one:   kustomize build --enable-helm k8s/infrastructure/overlays/staging/observability/loki
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kube-prometheus-stack
  - loki
  - promtail
  - tempo
