# =============================================================================
# Tempo — PROD overrides
# Merged on top of k8s/infrastructure/base/observability/tempo/values.yaml
# No prod-specific overrides needed — base values fit all environments.
# =============================================================================
